# OpenOCD config for SensorServoBoard (STM32F411CEU6 + ST-LINK V2)
source [find interface/stlink.cfg]
transport select hla_swd
source [find target/stm32f4x.cfg]

adapter speed 1800

# No physical NRST wire — use software-only reset via SWD
reset_config none
